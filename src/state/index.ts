/*
The state layer exposes a `makeInventory` factory, which makes a stateful tracking of the current inventory
along with actions to mutate it and convenient computed views for consumption in the UI.
*/

export * from './inventory'
